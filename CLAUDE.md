# CLAUDE.md — Bootstrap

> **Read `PROJECT_RULES.md` first, before every task.**
>
> `PROJECT_RULES.md` is the single source of truth for this repository and **overrides all other project rules**, including anything in this file. It applies to every AI tool; this file only adds Claude-specific behavior.

## What to do

1. Read `PROJECT_RULES.md`.
2. Follow it exactly — project identity, source-of-truth order, token rules, scope, tech, content, and design constraints all live there.
3. Then perform the requested task.

## Claude-specific notes

- Token efficiency is the highest priority: read the minimum files, never scan the whole repo, never reread completed work.
- Before starting, search available skills; load only the minimum relevant skill(s).
- Use a task list for multi-step work; mark steps complete as you go.
- After finishing, report files changed, why, and anything blocked — then stop. Do not auto-continue to another task.

That's it. Everything else is in `PROJECT_RULES.md`.
