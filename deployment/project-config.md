# Project Configuration Reference

Technical snapshot of the project as deployed. Update this file when dependency versions,
routes, external services, or build configuration change.

---

## Stack

| Component | Version |
|-----------|---------|
| Next.js | 16.2.9 (App Router) |
| React | 19.2.4 |
| TypeScript | 5.x |
| Tailwind CSS | 4.x |
| Node.js (required) | ≥ 20 (see `.nvmrc`) |

---

## Build

| Setting | Value |
|---------|-------|
| Build command | `npm run build` |
| Publish directory | `.next` |
| Dev server | `npm run dev` → `http://localhost:3000` |
| Production server | `npm run start` |
| Type check only | `npx tsc --noEmit` |

---

## Routes

| Path | File | Notes |
|------|------|-------|
| `/` | `app/page.tsx` | Homepage — all Phase 1 sections |
| `/solutions` | `app/solutions/page.tsx` | Full solutions and pricing page |
| `/sitemap.xml` | `app/sitemap.ts` | Auto-generated by Next.js |
| `/robots.txt` | `app/robots.ts` | Auto-generated by Next.js |

No dynamic routes. No API routes. No auth. No database.

---

## External Services

| Service | Purpose | Configured via |
|---------|---------|----------------|
| Netlify Forms | Contact form submissions | `data-netlify="true"` on the form — no env var required |
| Netlify | Hosting and CDN | See [netlify.md](netlify.md) |
| Google Fonts | Self-hosted at build time by `next/font` — no external CDN request at runtime | n/a |

No other external APIs, CMS, analytics, or third-party scripts.

---

## Environment Variables

| Variable | Required | Purpose |
|----------|----------|---------|
| `NEXT_PUBLIC_SITE_URL` | Yes (production build fails without it) | Canonical URL, OG tags, sitemap, robots |

Full details in [env-vars.md](env-vars.md).

---

## Security Headers

Set in `next.config.ts` and applied to all routes (`/(.*)`):

| Header | Value |
|--------|-------|
| `X-Content-Type-Options` | `nosniff` |
| `X-Frame-Options` | `DENY` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `Permissions-Policy` | `camera=(), microphone=(), geolocation=(), payment=()` |
| `Strict-Transport-Security` | `max-age=31536000; includeSubDomains` |
| `Content-Security-Policy` | See below |

**CSP summary:**
- `default-src 'self'`
- `script-src 'self' 'unsafe-inline' 'unsafe-eval'` — required by Next.js RSC
- `style-src 'self' 'unsafe-inline'` — required by Tailwind
- `img-src 'self' data: blob:` — `data:` for SVG grain texture; `blob:` for Next.js image internals
- `font-src 'self'` — Google Fonts are self-hosted at build time
- `connect-src 'self'` — contact form fetch to same origin (Netlify Forms)
- `form-action 'self'`
- `frame-ancestors 'none'`
- `object-src 'none'`

> If adding analytics or a third-party script in future, add its domain to `connect-src`
> (and `script-src` if loading an external JS file). See [maintenance.md](maintenance.md).

---

## Image Configuration

Configured in `next.config.ts`:
- Output formats: `image/avif`, `image/webp` (Next.js serves the best format the browser supports)
- All images use the Next.js `<Image>` component with `fill` or explicit dimensions

Image assets live in `public/images/`. See [maintenance.md](maintenance.md) for how to update them.

---

## Key Source Files

| File | Purpose |
|------|---------|
| `data/site.ts` | `siteConfig` (name, email, bookingUrl) and `navLinks` |
| `data/packages.ts` | Homepage package tiers (referenced by `Packages.tsx`) |
| `data/services.ts` | Services section content |
| `data/projects.ts` | Work/portfolio items |
| `data/socials.ts` | Social media links |
| `lib/types.ts` | Shared TypeScript types |
| `app/layout.tsx` | Root layout, metadata, JSON-LD structured data |
| `app/globals.css` | Design tokens (colors, spacing, typography) |
| `next.config.ts` | Security headers, image formats |
